<?xml version="1.0" encoding="UTF-8" ?>

<odoo>

    <record id="patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"></field>
                            <field name="age"></field>
                        </group>
                        <group>
                            <field name="gender"></field>
                            <field name="note"></field>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="patient_action" model="ir.actions.act_window">
        <field name="name">Judul Patients 1</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first patient!
            </p>
        </field>
    </record>

    <menuitem id="hospital_root" name="Hospital" sequence="10"/>

    <menuitem id="hospital_patient_root" name="Patients 1" parent="hospital_root" sequence="10"/>

    <menuitem id="hospital_patient" name="Patients 2" parent="hospital_patient_root" sequence="10" action="patient_action"/>
</odoo>